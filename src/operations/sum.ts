/**
 *
 * This method computes the sum of two numbers
 * @param {number} a
 * @param {number} b
 * @returns {number} returns the result of a + b
 */
function sum(a: number, b: number): number {
  return a + b;
}

export { sum as default };
